{
  "name": "clothing-workshop-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx wrangler dev",
    "type-check": "tsc --noEmit",
    "deploy:staging": "npx wrangler deploy --env staging",
    "deploy:production": "npx wrangler deploy --env production",
    "db:new": "npx wrangler d1 migrations create workshop_local",
    "db:migrate:local": "npx wrangler d1 migrations apply workshop_local --local",
    "db:migrate:staging": "npx wrangler d1 migrations apply workshop_staging --env staging",
    "db:migrate:production": "npx wrangler d1 migrations apply workshop_production --env production"
  },
  "dependencies": {
    "@clerk/backend": "^1.3.0",
    "@hono/clerk-auth": "^2.0.1",
    "@hono/zod-validator": "^0.3.0",
    "drizzle-orm": "^0.32.0",
    "hono": "^4.4.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240524.0",
    "drizzle-kit": "^0.22.0",
    "typescript": "^5.4.5",
    "wrangler": "^4.90.1"
  }
}
